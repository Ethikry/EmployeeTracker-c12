class Department {
    constructor (name, id) {
        this.name = name;
        this.id = id;
    };

    // getId() {
    //     return departmentsArr.indexOf(this);
    // };
};

module.exports = Department;

// THEN I am presented with a formatted table showing department names and department ids